{
    // General Settings

    // Position TOP
    "layer": "top",
    "position": "top",
    "margin-top": 10,
    "margin-bottom": 0,
    "height": 37,
    "margin-left": 0,
    "margin-right": 0,    
    "spacing": 0,

    // Load Modules
    "include": ["~/.config/waybar/modules.json"],

    // Modules Left
    "modules-left": [
        "clock"
    ],

    // Modules Center
    "modules-center": [
        "hyprland/workspaces"
    ],

    // Modules Right    
    "modules-right": [
		"tray",
        "bluetooth",
        "pulseaudio",
        "group/hardware",
        "battery", 
        "custom/exit"
    ],

    // Group Hardware
    "group/hardware": {
        "orientation": "horizontal",
        "modules": [
            "disk",
            "cpu",
            "memory"
        ]
    }
}
